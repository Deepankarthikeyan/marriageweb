{
  "name": "lavanya-balaji-wedding",
  "private": true,
  "version": "2.0.0",
  "description": "Premium static Hindu wedding invitation — HTML, CSS, JavaScript",
  "scripts": {
    "build": "bash scripts/build-site.sh",
    "deploy": "npm run build && npx wrangler pages deploy _site --project-name lavanya-balaji-wedding",
    "deploy:pages": "bash scripts/deploy-gh-pages.sh",
    "preview": "python3 -m http.server 8765"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
