{
  "name": "lavanya-balaji-wedding",
  "private": true,
  "version": "2.0.0",
  "description": "Premium static Hindu wedding invitation — HTML, CSS, JavaScript",
  "scripts": {
    "deploy": "npx wrangler deploy --config wrangler.worker.toml --temporary"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
