{
  "name": "lavanya-balaji-wedding",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy --config wrangler.worker.toml --temporary",
    "add-photo": "bash scripts/add-couple-photo.sh"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
